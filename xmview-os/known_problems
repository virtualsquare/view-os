20090806
pcb_allocation/deallocation can run in O(1), now there is a linear search

submachines (nested umview) may leave data around when terminate, 
the termination cleanup is not complete.

hashtab supports a negative mount mechanism for a fast management of
mount exceptions. All the modules still use a confirmation function.
